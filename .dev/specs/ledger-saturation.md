# Track: ledger-saturation — the last wall: irreversible reset observers over ℤ

Worker: gpt-5.6-sol, herdr worktree, branch `track/ledger-saturation`.
LOG.md at worktree root; first action: add to `.git/info/exclude`,
verify with `git check-ignore LOG.md`. Friction journal as always.
**The out:** this is the HARD track — the named open wall. A
sharpened wall (minimal counterexample, or the exact missing
lemma made smaller) is a full-value deliverable. Do not weaken
statements to fake progress. > ~30 min per sub-question: record,
move on.

## Context

Classification report round 2 (`aips/draft/cocycle-classification.md`,
commit 88d9db6): the local-to-global theory covers W=P=id
(complete), strongly connected (ℚ), and permutation observers
(integral, index divides group exponent). ONE wall remains: "a
uniform exponent/saturation theorem for the reachable
signed-cycle event ledger" of general irreversible reset
observers — naive Noetherian descent fails because descending
chains of ℤ-sublattices need not stabilize. You (this pane's
earlier rounds) built that theory; this track attacks the wall.

## Deliverable: `aips/draft/ledger-saturation.md` (+ PDF per AGENTS.md recipe)

1. **Name the object.** Define the "reachable signed-cycle event
   ledger" precisely and standalone (the report gestures at it;
   make it a definition someone could formalize): presumably the
   sub-lattice of ℤ^(Σ×Q-ish event space) generated by signed
   cycles reachable at grade n, with the maps induced by the
   left-subtree embedding.
2. **Attack routes, in order of expected yield:**
   a. *Two-phase decomposition*: an irreversible reset observer
      factors reachable dynamics into strongly-connected
      components ordered by irreversible transitions (first-touch:
      U-phase → S-phase). Within each component the round-2
      strongly-connected theory applies; the question reduces to
      finitely many component-transition corrections. Does the
      component DAG stabilize under the left-subtree embedding?
   b. *Index bound via monoid structure*: the transformation
      monoid of the observer is finite and grade-independent;
      its maximal subgroups have bounded exponent. Conjecture:
      arithmetic indices divide the lcm of the exponents of the
      maximal subgroups (the permutation case proved; resets add
      only aperiodic structure, which should contribute NO new
      torsion — aperiodic monoids have trivial subgroups.
      Krohn–Rhodes flavor: torsion comes only from the group
      part). If true this kills the integral wall entirely.
   c. *Minimal counterexample hunt*: if (b) fails, find the
      smallest observer where indices grow with grade — that
      would be a genuine phase-transition result and equally
      publishable.
3. **Consequence bookkeeping**: whichever way it lands, state
   exactly which slice of the tower theory becomes complete, and
   restate the report's dekkingskaart (coverage map) with the
   new boundary.
4. **Lean-readiness note**: which parts are finite/checkable
   (component DAGs at small grade) vs genuinely infinitary.

## Out of scope
The comparison and pricing-surface questions (parallel tracks).
Only your report + LOG.

## Gate
PDF renders (pandoc recipe); claims labeled; expos untouched and
still compiling. Commit on the track branch only.
